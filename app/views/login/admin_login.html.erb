                    <%= form_tag({:controller => "login", :action => "admin_login"}, :method => "post") %>
                        <fieldset>
                            <div class="form-group">
                                <%= text_field(:user, :email, :class => "form-control", :placeholder => "Email Address", :autofocus => true ) %>
                            </div>
                            <div class="form-group">
                                <%= password_field(:user, :password, :class => "form-control", :placeholder => "Password") %>
                            </div>
                            <input type="hidden" value="<%= params['order'] %>" name="order" />                        
                            <!-- Change this to a button or input when using this as a form -->
    
                            <button type="submit" class="btn btn-lg btn-success btn-block">Admin Login</button>
                            <div class="reset_links"><a href="/login/recovery/admin">Reset Password</a> | <a href="/login/clothier<% if params['order'] %>?order=<%= params['order'] %><% end %>">Switch to Clothier</a></div>
                        </fieldset>
                    </form>
                    <% if @message != "" and @message != nil %><p style="text-align:center;padding:15px"><%= @message %></p><% end %>